# Yerel VPS: host'tan erişim için kopyalayın:
#   cp docker-compose.override.example.yml docker-compose.override.yml
# Coolify kullanıyorsanız bu dosyaya gerek yok.

services:
  web:
    ports:
      - "${WEB_PORT:-8000}:8000"
